# DevTime — Система учёта рабочего времени

> Приложение на PHP + PostgreSQL для учёта рабочего времени IT-команды.

---

## 1. Файловая структура

```
/
├── index.php        — Точка входа. Роутер по роли.
├── login.php        — Аутентификация.
├── logout.php       — Уничтожение сессии.
├── data.php         — Ядро: настройки БД, все запросы, хелперы.
├── actions.php      — Обработчик смен (start / stop / break / resume).
├── dashboard.php    — Дашборд. Панель управления + таблица учёта времени.
├── team.php         — Страница команд.
├── reports.php      — Экспорт отчёта в CSV.
├── admin.php        — Панель администратора. CRUD пользователей и команд.
├── initbase.php     — Автоматически создаёт базу и схему при первом запуске.
└── schema.sql       — Схема БД для ручного выполнения в DBeaver (опционально).
```

---

## 2. Быстрый старт

### Требования
- PHP >= 8.0
- PostgreSQL >= 13

### Шаг 1 — Включи расширения PHP

Найди `php.ini` (путь покажет `<?php phpinfo();`) и раскомментируй:

```ini
extension=pdo_pgsql
extension=pgsql
```

Перезапусти сервер после изменений.

### Шаг 2 — Настрой подключение к БД

В файле `data.php` в самом верху:

```php
define('DB_HOST', '127.0.0.1');
define('DB_PORT', '5432');
define('DB_NAME', 'devtime');      // имя базы
define('DB_USER', 'postgres');     // пользователь
define('DB_PASS', 'твой_пароль'); // пароль
```

### Шаг 3 — Запусти сервер

```bash
cd /путь/к/проекту
php -S localhost:8000
```

### Шаг 4 — Инициализируй базу

Открой в браузере `http://localhost:8000` — если база не существует, произойдёт автоматический редирект на `initbase.php`, который создаст базу, схему и всех дефолтных пользователей. После этого нажми «Перейти к входу».

> Повторный запуск `initbase.php` безопасен — если таблицы уже есть, ничего не тронет.

---

## 3. Роли и права доступа

| Роль | Логин | Точка входа | Доступные страницы |
|---|---|---|---|
| Администратор | `admin` | `admin.php` | только `admin.php` |
| Тим Лид | `teamlead` | `dashboard.php` | dashboard, team, reports |
| Сотрудник | `maria` и др. | `dashboard.php` | dashboard, team, reports |

---

## 4. Схема базы данных

| Таблица | Назначение |
|---|---|
| `roles` | Справочник ролей: admin, teamlead, employee |
| `users` | Пользователи |
| `user_roles` | Связь пользователей и ролей |
| `teams` | Команды |
| `team_members` | Участники команд |
| `work_logs` | Журнал рабочего времени (интервальный подход) |
| `daily_reports` | Итоги дня: суммарные минуты, переработка |
| `overtime_requests` | Заявки на переработку (pending / approved / rejected) |

### Логика work_logs

Каждая строка — один непрерывный отрезок работы или перерыва:

| Действие | Что происходит в БД |
|---|---|
| «Начать» | INSERT started_at = NOW(), ended_at = NULL |
| «Пауза» | UPDATE ended_at = NOW() + INSERT type = 'break' |
| «Продолжить» | UPDATE ended_at = NOW() на перерыве + INSERT type = 'work' |
| «Стоп» | UPDATE ended_at = NOW() + пересчёт daily_reports |

Переработка считается автоматически при «Стоп»: если сумма рабочих минут за день > 480 (8 часов), остаток записывается в `overtime_requests` со статусом `pending`.

---

## 5. Логика видимости по ролям

### Dashboard
| Что | Тим Лид | Сотрудник |
|---|---|---|
| Карточки управления | Все сотрудники | Только своя |
| Таблица учёта | Все сотрудники | Только своя строка |
| Итоговые карточки | Агрегат по всем | Только свои данные |

### Team
| Что | Тим Лид | Сотрудник |
|---|---|---|
| Видит команды | Все где состоит | Команды где состоит |
| CRUD команд | Да | Нет |

### Reports
Тим Лид получает все записи, сотрудник — только свои.

---

## 6. Исправленные ошибки (code review)

### Критические

| Файл | Проблема | Исправление |
|---|---|---|
| `team.php` | CRUD команд работал через `$_SESSION` вместо БД (TODO-заглушки) | Переписан на реальные SQL-запросы с проверкой прав тим лида |
| `schema.sql` / `initbase.php` | В таблице `overtime_requests` отсутствовал `UNIQUE (user_id, report_date)`, из-за чего `ON CONFLICT` в `data.php` падал с ошибкой | Добавлен UNIQUE constraint в обоих файлах |
| `data.php` | `ON CONFLICT DO NOTHING` без указания колонок конфликта — невалидный синтаксис PostgreSQL | Исправлено на `ON CONFLICT (user_id, report_date) DO NOTHING` |

### Безопасность

| Файл | Проблема | Исправление |
|---|---|---|
| `logout.php` | `session_destroy()` без предварительной очистки данных сессии и удаления cookie — сессия могла не уничтожаться полностью | Добавлены `$_SESSION = []`, удаление cookie и проверка `session_status()` |
| `actions.php` | `employee_name` из POST передавался в URL редиректа без санитизации — потенциальный XSS | Добавлены `htmlspecialchars` + `strip_tags` перед передачей в заголовок |

### Логические ошибки

| Файл | Проблема | Исправление |
|---|---|---|
| `admin.php` | При отображении карточек команд тим лид искался как первый попавшийся пользователь с ролью `teamlead` из всех пользователей, а не из участников конкретной команды | Поиск переключён на `$members` (участники текущей команды) |
| `team.php` | Тернарный оператор в заголовке `'Мои команды' : 'Мои команды'` — обе ветки одинаковые | Исправлено на `'Мои команды' : 'Моя команда'` |

### Мелкие неточности

| Файл | Проблема | Исправление |
|---|---|---|
| `dashboard.php` | Живые часы обновлялись каждые 10 секунд, хотя отображаются только HH:MM | Интервал изменён на 60 секунд |

---

## 7. Тестовые учётные записи

| Логин | Пароль | Роль | Команда |
|---|---|---|---|
| `admin` | `admin123` | Администратор | — |
| `teamlead1 ИЛИ teamlead2` | `lead123` | Тим Лид | Frontend Squad, Backend Guild |
| `maria` | `pass123` | Сотрудник | Frontend Squad |
| `dmitry` | `pass123` | Сотрудник | Frontend Squad |
| `elena` | `pass123` | Сотрудник | Backend Guild |
| `pavel` | `pass123` | Сотрудник | Backend Guild |

---

## 8. Доработки и рефакторинг (второй этап)

### Исправления безопасности и прав доступа

| Файл | Изменение |
|---|---|
| `admin.php` | Администратор не может изменить свою роль (защита от самопонижения). В карточке своего профиля скрыты поля роли и команды; в карточках других сотрудников скрыты поля логина и пароля |
| `team.php` | Тим лид лишён кнопок создания, редактирования и удаления команд и участников — только просмотр |

### Новый функционал

| Файл | Изменение |
|---|---|
| `dashboard.php` | В таблице учёта времени добавлены кнопки ✓ / ✗ в колонке «Переработка». Тим лид может подтвердить или отклонить переработку прямо из интерфейса |
| `actions.php` | Добавлены обработчики `approve_overtime` и `reject_overtime` — обновляют статус записи в `overtime_requests` |
| `data.php` | Добавлена функция `getOvertimeRequest()` — возвращает текущую заявку на переработку сотрудника за сегодня |
| `actions.php` | При повторном нажатии «Начать работу» все незакрытые записи за день закрываются перед созданием новой — время начала корректно обновляется |
| `dashboard.php` | Фильтры таблицы учёта времени реализованы: выбор периода (текущая неделя / прошлая неделя / текущий месяц) и фильтр по конкретной дате — применяются через GET-параметры |
| `data.php` | Функции `getEmployees()`, `getDailyReport()`, `getWeeklyHours()` принимают параметры даты и периода для поддержки фильтрации |

### Рефакторинг структуры

| Файл | Изменение |
|---|---|
| `style.css` | Создан единый файл стилей для всего проекта. Убраны дублирующиеся inline `<style>` блоки из всех страниц |
| `layout.php` | Создан общий шаблон header + date-bar. Все страницы (`dashboard.php`, `team.php`, `reports.php`, `admin.php`) подключают его через `require 'layout.php'` |
| `dashboard.php` | Убраны всплывающие уведомления полностью |

### Исправления отчётов

| Файл | Изменение |
|---|---|
| `reports.php` | Добавлен `ob_end_clean()` перед отправкой CSV-заголовков — исключает конфликт с буферизованным HTML-выводом |
| `reports.php` | Дата в отчёте переведена в формат `dd.mm.yyyy` для корректного отображения в Excel |
| `reports.php` | Часы за день округляются до 1 знака после запятой |

---

## 9. Валидация и защита данных

| Файл | Изменение |
|---|---|
| `admin.php` | Проверка уникальности логина при создании и редактировании пользователя — дублирующий логин отклоняется с сообщением об ошибке |
| `admin.php` | Проверка длины полей: логин и пароль до 50 символов, должность и проект до 50, ФИО до 100 |
| `admin.php` | Проверка на конфликт тимлидов: нельзя добавить второго тимлида в команду где уже есть тимлид — при создании и при редактировании |
| `admin.php` | Сообщения об ошибках отображаются красным, сообщения об успехе — зелёным |
| `data.php` | Тимлид видит на дашборде только сотрудников своих команд; если не прикреплён ни к одной команде — таблица пуста |
